CALC SQUARE — ПРОВЕРКА И СЛИЯНИЕ
База: ваша версия build 2026-06-24-1058
================================================================

ИТОГ: ошибок нет, всё ваше новое корректно и проходит ваш
validate-publish.ps1. Вернул check-roles.js (его в архиве не было).

1. ОШИБКИ — не найдено
----------------------------------------------------------------
- Синтаксис panel-module.js / preview-axo.js — чистый.
- Запрещённых внешних зависимостей нет (googleapis/gstatic/cdnjs/
  jsdelivr/@fontsource/@import/внешние <script>) — валидатор пройдёт.
- console.log / debugger / TODO / FIXME — нет.
- Мёртвые файлы (preliminary-module.js, module.html) удалены — чисто.

2. ВЗЯЛ ВАШЕ НОВОЕ (то, чего у нас не было)
----------------------------------------------------------------
Это ваша новая работа, оставил как есть — она и есть «лучшее»:
- rectDuct: отдельный расчёт прямоугольного воздуховода с логикой
  замка/сварки (Z1/Z2, имя замка «Сварка»/«Американский») и раскроем
  листа 1245×2960.
- Гостевой счётчик остатка: «осталось {left} из {limit}» и сообщение
  при исчерпании лимита.
- Поле «Комментарий» (для не-гостя), сохраняется в URL, обновляет
  расчёт на ввод.
- canViewFormulas = только admin (раньше — все, кроме client).
  Ваш validate-publish.ps1 синхронно это проверяет — согласовано.
- Новые i18n-ключи (angle/main/branch/avgDiameter/inclinedLength/
  avgPerimeter, optionsReserved, formulaHelpPending) во всех 3 языках.

3. БЕЗОПАСНОСТЬ — ОК
----------------------------------------------------------------
- escapeHtml на всех пользовательских полях (имя/описание/комментарий/
  размеры) — XSS нет.
- Внешних ресурсов нет. eval/new Function/document.write — нет.
- Напоминание: роли — это разграничение интерфейса, не защита
  (на статике любой откроет ?role=admin). Если формулы секретны —
  на клиенте их не спрятать.

4. ВЕРНУЛ check-roles.js
----------------------------------------------------------------
В присланном архиве его снова не было (лежит у вас на диске).
Положил исправленную версию:
- читает метку текстом (не ждёт видимости hidden);
- запускает системный Chrome/Edge (не требует playwright install);
- без console.log (нет WARNING в валидаторе);
- ОЖИДАНИЯ ПО РОЛЯМ обновлены под ваш движок:
    guest  — добавление НЕТ
    user   — добавление ДА
    client — добавление ДА
    admin  — добавление ДА
    PDF — не ожидается ни у кого (вы PDF удалили).
  Это убирает тот FAIL у client/admin (pdf=false(true)), что был
  на скриншоте.

5. ЛИШНЕЕ (не критично)
----------------------------------------------------------------
- i18n-ключи "pdf" (ru/uk/en) снова присутствуют в panel-module.js,
  но PDF-кнопки и экспорта нет — это мёртвые ключи. Можно убрать.
  (Не трогал, т.к. это ваш файл; см. вопрос 1.)

6. ВОПРОСЫ
----------------------------------------------------------------
1) PDF точно не нужен? Если да — уберу мёртвые ключи "pdf".
   Если захотите вернуть — учтите: jsPDF из CDN нельзя (cdnjs/jsdelivr
   в запрете вашего валидатора), нужен локальный jsPDF.
2) canViewFormulas = admin: теперь и user не видит формулы (раньше
   видел). Так задумано?
3) preview.svg по модулям — нужны или удалить (превью даёт preview-axo)?

----------------------------------------------------------------
Сборку НЕ менял — контент сайта не трогал, только добавил
scripts/check-roles.js. Ваш build 2026-06-24-1058 остаётся;
publish.bat обновит его сам при публикации.
